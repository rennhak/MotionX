#!/usr/bin/ruby

# File: <%= @filename %>.rb

<%
  ###
  # NOTICE FOR DEVELOPERS:
  #
  # Access all members of the XYAMLSpecification.yaml file via @data.<membername>...
  # E.g. @data.metadata.motion.name etc.
  #######
%>

###
#
# This is a autogenerated XYAML Ruby Interface file.
#
# WARNING:
#   Please don't try to change this file as this will be lost when it is re-generated.
#   If you really must, create diff patches or patch the XYAMLSpecification.yaml and/or GenSpec.rb and/or templates directly.
#
######


# = Libraries
require 'rubygems'
require 'ostruct'
require 'yaml'



# = The <%= @filename %> Class is a XYAML Ruby Interface generated from the XYAMLSpecification.yaml file via GenSpec.rb.
# In good OOP manner every section below the root in the XYAML Spec is its own class. e.g.
# .metadata -> .sound , .motion etc.
class <%= @filename %>

  # = Initialize function for the autogenerated XYAML Class
  # @param specPath Expects a sane path without a trailing slash
  # @param specFile Expects a sane @filename with .yaml extension
  # @param version Expects a string containing the version of the XYAML spec file used to generate this file
  def initialize specPath = "<%= @specPath %>", specFile = "<%= @specFile %>", version = "<%= @d.version %>"
    @specPath, @specFile = specPath, specFile
    @version             = version

    # Metadata.motion
    @name           = ""            # e.g. <%= @d.metadata.motion.name          %>
    @category       = ""            # e.g. <%= @d.metadata.motion.category      %>
    @by             = ""            # e.g. <%= @d.metadata.motion.by            %>
    @gender         = ""            # e.g. <%= @d.metadata.motion.gender        %>
    @capturedTime   = DateTime.new  # e.g. <%= @d.metadata.motion.capturedTime  %>
    @capturedBy     = ""            # e.g. <%= @d.metadata.motion.capturedBy    %>
    @clothes        = ""            # e.g. <%= @d.metadata.motion.clothes       %>
    @utils          = ""            # e.g. <%= @d.metadata.motion.utils         %>
    @contact        = ""            # e.g. <%= @d.metadata.motion.contact       %>

    # Metadata.capture
    @device         = ""            # e.g. <%= @d.metadata.capture.device       %>
    @way            = ""            # e.g. <%= @d.metadata.capture.way          %>
    @sensors        = 0             # e.g. <%= @d.metadata.capture.sensors      %>
    @area           = []            # e.g. <%= @d.metadata.capture.area         %>
    @placement      = ""            # e.g. <%= @d.metadata.capture.placement    %>

    # Metadata.sound
    @file           = ""            # e.g. <%= @d.metadata.sound.file           %>


  end


  attr_accessor :specPath, :specFile, :version, :name, :category, :by, :gender, :capturedTime, :capturedBy, :clothes, :utils, :contact

end # <%= @filename %>


class Sound
  def initialize
  end

end # Sound


# vim=ts:2

